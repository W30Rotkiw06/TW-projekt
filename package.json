{
  "name": "tw-projekt",
  "version": "1.0.0",
  "description": "A small business website using Tailwind CSS",
  "main": "src/index.html",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./docs/style.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./src/style.css --watch",
    "deploy": "npm run build:css && cp -r src/*.html docs/ && cp -r assety docs/ && git add docs && git commit -m 'Deploy to GitHub Pages' && git push"
  },
  "dependencies": {
    "tailwindcss": "^3.0.0",
    "postcss": "^8.0.0",
    "autoprefixer": "^10.0.0"
  },
  "devDependencies": {
    "postcss-cli": "^8.0.0"
  },
  "keywords": [
    "tailwindcss",
    "small business",
    "website"
  ],
  "author": "Your Name",
  "license": "MIT"
}